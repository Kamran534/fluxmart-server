# FluxMart Server - Environment Helper
# Copy needed keys into your .env and set real values.

# Core Server
NODE_ENV=                              # Node environment (development|production)
PORT=                                  # HTTP server port

# CORS & Security
CORS_ALLOWED_ORIGINS=                   # Comma-separated allowed origins
RATE_LIMIT_WINDOW_MS=                   # Rate-limit window in ms (default 15m)
RATE_LIMIT_MAX=                         # Max requests per IP per window

# MongoDB (Mongoose)
MONGODB_URI=
MONGODB_DB_NAME=                        # Optional; overrides DB name in URI
MONGODB_MAX_POOL_SIZE=                  # Connection pool size (max)
MONGODB_MIN_POOL_SIZE=                  # Connection pool size (min)
MONGODB_SRV_TIMEOUT_MS=                 # Server selection timeout (ms)
MONGODB_SOCKET_TIMEOUT_MS=              # Socket timeout (ms)
MONGODB_APP_NAME=                       # App name for diagnostics

# Redis
REDIS_URL=
REDIS_MAX_RETRIES=                      # Max connect retries
REDIS_RETRY_DELAY_MS=                   # Initial retry delay (ms), exponential backoff

# SMTP / Email
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=                            # true for 465, false otherwise
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
REPLY_TO_EMAIL=
COMPANY_NAME=
COMPANY_WEBSITE=
SUPPORT_EMAIL=
SMTP_REJECT_UNAUTHORIZED=               # TLS cert verification; true in production

# Swagger
SWAGGER_SERVER_LOCAL=
SWAGGER_SERVER_PROD=                    # e.g., https://api.fluxmart.com (leave empty to omit)

# GraphQL
GRAPHQL_MAX_DEPTH=                      # Max GraphQL query depth (prod hardening)
